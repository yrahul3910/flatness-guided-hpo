import numpy as np


scores = []
for file in list`ls *.out`:
    print("Parsing", file)
    line = `tail -n1 {file}`
    line = eval(line.split(':')[1])[0]
    scores.append(line)

print('Scores:', scores)
print('Median:', np.median(scores))
